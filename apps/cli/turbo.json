{
  "extends": ["//"],
  "tasks": {
    "prepublishOnly": {
      "dependsOn": ["^build"],
      "env": [
        "OPENCOMPOSER_VERSION",
        "PUBLISH_PACKAGES",
        "RELEASE_OPENCOMPOSER_ZIPS",
        "GITHUB_SHA",
        "GITHUB_HEAD_REF",
        "GITHUB_REF_NAME"
      ]
    }
  }
}
