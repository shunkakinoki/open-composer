{
  "extends": ["//"],
  "tasks": {
    "prepublishOnly": {
      "dependsOn": ["^build"],
      "env": [
        "OPENCOMPOSER_VERSION",
        "PUBLISH_PACKAGES",
        "RELEASE_OPENCOMPOSER_ZIPS"
      ]
    }
  }
}
