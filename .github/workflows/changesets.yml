
name: Changesets
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.sha }}
  cancel-in-progress: true
jobs:
  changesets-publish:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
      issues: write
      repository-projects: write
      deployments: write
      packages: write
      actions: write
      checks: write
      statuses: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Bun
        uses: shunkakinoki/actions/.github/actions/setup-bun@36c30e5cf16d45445c026abf8f71a437443cbd33
      - name: Set Environment Variables
        run: |
          echo "PUBLISH_PACKAGES=${PUBLISH_PACKAGES}" >> $GITHUB_ENV
          echo "RELEASE_OPENCOMPOSER_BINS=${RELEASE_OPENCOMPOSER_BINS}" >> $GITHUB_ENV
          echo "RELEASE_OPENCOMPOSER_ZIPS=${RELEASE_OPENCOMPOSER_ZIPS}" >> $GITHUB_ENV
          echo "PREPARE_OPENCOMPOSER_RELEASE=${PREPARE_OPENCOMPOSER_RELEASE}" >> $GITHUB_ENV
        env:
          PUBLISH_PACKAGES: true
          RELEASE_OPENCOMPOSER_BINS: true
          RELEASE_OPENCOMPOSER_ZIPS: true
          PREPARE_OPENCOMPOSER_RELEASE: true
      - name: Run Changesets
        uses: shunkakinoki/actions/.github/actions/changesets@36c30e5cf16d45445c026abf8f71a437443cbd33
        with:
          github-token: ${{ secrets.PAT_TOKEN }}
          npm-token: ${{ secrets.NPM_ORGANIZATION_TOKEN }}
          publish-command: 'bun run publish'
          create-github-releases: 'true'
          commit-mode: 'git-cli'
  changesets-pull-request:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Setup Bun
        uses: shunkakinoki/actions/.github/actions/setup-bun@36c30e5cf16d45445c026abf8f71a437443cbd33
      - name: Run Prepublish
        run: bun run prepublishOnly
      - name: Run Changesets
        uses: shunkakinoki/actions/.github/actions/changesets@ed8f880cce1692b9d04df6dac1ab556a17641ff3
        continue-on-error: true
        with:
          validate-pull-request: 'true'
  changesets-check:
    if: always()
    needs:
      - changesets-publish  
      - changesets-pull-request
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Alls Green
        uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
          allowed-skips: changesets-publish,changesets-pull-request
